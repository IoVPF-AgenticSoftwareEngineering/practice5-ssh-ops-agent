# 同期設定ファイル
# このファイルは Claude Code の bulk-deploy-sync-workflows コマンドによって生成されました

sync:
  # テンプレートリポジトリ（同期先）
  template_repo: "IoVPF-AgenticSoftwareEngineering/agents-startup"

  # 同期先ブランチ
  target_branch: "feature/agents-startup_0.2.1"

  # 同期元ブランチ（このリポジトリ）
  source_branch: "research/ToDo_AI_tracking_check"

  # 同期先ディレクトリ（テンプレートリポジトリ内）
  target_dir: "commit_message_rules"

  # 同期対象（このリポジトリ内のパス）
  targets:
    - path: "ai-usage-tracking-guide-for-beginners.md"
